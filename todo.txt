0) Перенести все макросы в арху Win64x (в каждом проекте editor).
1) Собрать all_editors на новых компиляторах (далее ActorEditor.exe, LevelEditor.exe, все в Release Win64)
2) copy_lib_архитектура_конфигурация.cmd - запускать батник как пост событие xrEntry. Исправить батник, доложить в него новые нужные зависимости. 
Зависимости искать с помощью dumpbin после сборки. КОНВЕРТ В .a БОЛЕЕ НЕ ТРЕБУЕТСЯ!
3) избавиться от батника fix_headers_for_rad_studio.cmd
4) доработать gitignore - *.local, __astcache, __history, __recovery, .~(ext)
5) удалить Win32 из движка (каждый проект из sln)



В список изменений:
* отказ от Win32. Теперь RAD Studio IDE 64 - разрядная, что дало возможность перевести runtime - компоненты в Win64;
* поднят C++ до 23 для проектов RAD Studio.Без полной переработки кода, только точечные правки;
* поднят toolset до 145 (Visual Studio 2026). Без полной переработки кода, только точечные правки;
* запуск проверялся на Windows 11, глубокого тестирования не было. Возможны проблемы/артефакты. Будет время - займусь;
* проработан gitignore, удалены лишние/старые файлы/участки кода;
* оптимизирован процесс сборки, скорректирован ReadMe под это.