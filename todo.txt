1) Текущее:
- LE: вылет при выходе с любым открытым уровнем или не сохраненными данными

- LE: сделать уровень mp_combat сингл на тест (макс качество)
- xrLC: сборка уровеня mp_combat сингл на тест (макс качество)

- engine: тест уровня
- engine standart: тест уровня

- LE: пики в ai сети - устранить (отладка уровнем silent_backwater_first_full_build)

- AE: полный тест (smoke)
- LE: полный тест (smoke)

- весь код в UTF-8 с сохранением ру лок (комментарии)

2) Планы:
- LE: интеграция автонастройки материалов в редактор

В список изменений:
* отказ от Win32. Теперь RAD Studio 13.1 IDE (сборка настроена под эту версию) 64 - разрядная, что дало возможность перевести bpl runtime - компоненты в Win64;
* поднят C++ до 23 для проектов RAD Studio. Без полной переработки кода, только точечные правки;
* поднят toolset до 145 (Visual Studio 2026). Без полной переработки кода, только точечные правки;
* запуск проверялся на Windows 10/11, глубокого тестирования не было. Возможны проблемы/артефакты. Будет время - займусь;
* проработан gitignore, удалены лишние/старые файлы/участки кода;
* оптимизирован процесс сборки, скорректирован ReadMe под это;
* включен корректный скейлинг для всевозможных разрешений экранов;
* заменен Open/Save File/Folder Dialog на COM - реализацию;
* исправлен expand/collapse кнопки в ActorEditor и LevelEditor;
* множество мелких правок для стабилизации Win64;
* стабилизация рендера в LevelEditor при старте/завершении приложения;
* исправление падения при выходе из ActorEditor и LevelEditor;
* добавлен запрос UAC - прав (для исправления ошибок с записью в системные настройки и прочее)
* стабилизация Debug - конфигурации engine (легкая, ошибки еще есть, не играбельно) на ресурсах игры с патчем 1.6.02;
* стабилизация Release - конфигурации engine (проверялось в основном на Затоне) на ресурсах игры с патчем 1.6.02;
* LevelEditor: при загрузке карты, если на ней отсутствую spawns/static - объекты, то в диалоговом окне теперь есть кнопка прерывания загрузки уровня (Cancel);
* LevelEditor: исправление проблем при загрузке и воспроизведение анимаций из ogf - файлов;
* editors: Splash Screen теперь не блокирующий, с возможностью закрыть редакторы при загрузке;
* LevelEditor: исправление проблемы генерации травы;
* 
